# yaml-language-server: $schema=../../artifacts/json-schemas/project.json
name: omni_cli

tasks:
  list:
    command: exit 1
    dependencies:
      - ^test

  install:
    command: cargo install --path .
    dependencies:
      - list

dependencies:
  - omni_core
  - env_loader

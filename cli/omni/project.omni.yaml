# yaml-language-server: $schema=../../artifacts/json-schemas/project.json
name: omni_cli

tasks:
  test: cargo test -p omni

  build:
    command: carbo build --release --path .
    dependencies:
      - test

  install:
    command: cargo install --path .
    dependencies:
      - build

dependencies:
  - omni_core
  - env_loader
  - config_utils
  - omni_types

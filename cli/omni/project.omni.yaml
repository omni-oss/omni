# yaml-language-server: $schema=../../artifacts/json-schemas/project.json
name: omni_cli

tasks:
  list:
    command: ls -la
    dependencies:
      - ^test

dependencies:
  - omni_core
  - env_loader

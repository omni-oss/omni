# yaml-language-server: $schema=https://raw.githubusercontent.com/omni-oss/json-schemas/refs/heads/main/project.json
name: "@omni-oss/runtime-utils"

extends:

  - "@workspace/omni/presets/ts-vite-lib.omni.yaml"


tasks:
  test:unit:
    enabled: true

  test:integration:
    enabled: false

  test:
    enabled: true

  build:
    enabled: true

  publish:
    enabled: true

meta:
  publish: true

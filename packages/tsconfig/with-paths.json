{
    "$schema": "https://json.schemastore.org/tsconfig.json",
    "extends": ["./base.json"],
    "compilerOptions": {
        "rootDir": "${configDir}/src",
        "paths": {
            "@/*": ["${configDir}/src/*"]
        }
    }
}

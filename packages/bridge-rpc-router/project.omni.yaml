# yaml-language-server: $schema=https://raw.githubusercontent.com/omni-oss/omni/refs/heads/json-schemas/project-latest.json
name: "@omni-oss/bridge-rpc-router"

extends:
  - "@workspace/omni/presets/ts-vite-lib.omni.yaml"

tasks:
  build:
    enabled: true

  test:unit:
    enabled: true

  test:integration:
    enabled: true

dependencies:
  - "@omni-oss/tsconfig"
  - "@omni-oss/vite-config"
  - "@omni-oss/vitest-config"
  - "@omni-oss/bridge-rpc"
  - "@omni-oss/channels"

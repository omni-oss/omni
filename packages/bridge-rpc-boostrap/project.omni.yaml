# yaml-language-server: $schema=https://raw.githubusercontent.com/omni-oss/json-schemas/refs/heads/main/project.json
name: "@omni-oss/bridge-js-boostrap"

extends:
  - "@workspace/omni/presets/ts-vite-lib.omni.yaml"

dependencies:
  - "@omni-oss/bridge-rpc-core"
  - "@omni-oss/bridge-rpc-router"

tasks:
  test:unit:
    enabled: true

  test:integration:
    enabled: true

  test:
    enabled: true

  build:
    enabled: true

  publish:
    enabled: false

meta:
  publish: false

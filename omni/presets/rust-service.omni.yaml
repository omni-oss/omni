# yaml-language-server: $schema=https://raw.githubusercontent.com/omni-oss/json-schemas/refs/heads/main/project.json
name: rust-service

extends:
  - ./rust.omni.yaml

tasks:
  build:
    exec: cargo build --release --package ${PROJECT_NAME} ${RUST_BUILD_ADDITIONAL_ARGS:-}
    cache:
      key:
        env:
          append: [RUST_BUILD_ADDITIONAL_ARGS]
    dependencies:
      - test
    output:
      files:
        append:
          - "@workspace/target/release/{{ env.PROJECT_NAME }}"
          - "@workspace/target/release/{{ env.PROJECT_NAME }}.exe"

  dev:
    interactive: true
    persistent: true
    cache:
      enabled: false
    exec: watchexec -d 2s -r --exts rs -w src cargo run --package ${PROJECT_NAME} ${ADDITIONAL_ARGS:-}

meta:
  type: service
  release:
    github: true

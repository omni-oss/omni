# yaml-language-server: $schema=../../artifacts/json-schemas/project.json
name: base

tasks:
  passthrough:
    description: Only exists so dependents can be re-executed based on the changes of a dependees

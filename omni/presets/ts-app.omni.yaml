# yaml-language-server: $schema=../../artifacts/json-schemas/project.json
name: ts-app

extends:
  - "./ts.omni.yaml"

tasks:
  dev:
    command: bun run dev
    # persistent: true

meta:
  type: app

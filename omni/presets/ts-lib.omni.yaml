# yaml-language-server: $schema=https://raw.githubusercontent.com/omni-oss/omni/refs/heads/json-schemas/project-latest.json
name: ts-lib

extends:
  - "./ts.omni.yaml"

tasks:
  publish:
    command: bun publish --tolerate-republish
    dependencies:
      - build
      - ^publish

meta:
  type: lib
  publish: true
  release:
    npm: true

name: project-1

output_logs: failed

tasks:
  run:
    exec: ls -la
    output_logs:
      new: all
    dependencies:
      - project-2#list
      - build

  build: echo "build"

dependencies:
  - project-2

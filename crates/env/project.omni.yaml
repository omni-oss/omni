# yaml-language-server: $schema=../../artifacts/json-schemas/project.json
name: env

extends:
  - ${WORKSPACE_DIR}/omni/presets/rust-lib.omni.yaml

tasks:
  profile:
    env:
      vars:
        bench: env_benchmarks

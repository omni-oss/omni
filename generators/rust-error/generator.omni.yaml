# yaml-language-server: $schema=../../.artifacts/generator.json
name: rust-error
display_name: Rust Error
description: Generate a new error type

inputs:
  - type: string
    name: error_name
    message: Error Name
    default: Error

targets:
  root: .

vars:
  rust_file_name: "{{ inputs.error_name | snake_case }}"

actions:
  - type: add
    base_path: templates
    target: root
    data:
      rust_error_name: "{{ inputs.error_name | upper_camel_case }}"
    file: templates/{{ vars.rust_file_name }}.rs.tpl

# yaml-language-server: $schema=https://raw.githubusercontent.com/omni-oss/json-schemas/refs/heads/main/project.json
name: "@omni-oss/bridge-service"

extends:
  - "@workspace/omni/presets/ts-vite-script.omni.yaml"

dependencies:
  append:
    - "@omni-oss/tsconfig"
    - "@omni-oss/vite-config"
    - "@omni-oss/vitest-config"
    - "@omni-oss/bridge-rpc-core"
    - "@omni-oss/bridge-rpc-bootstrap"
    - "@omni-oss/bridge-rpc-services"
    - "@omni-oss/bridge-rpc-utils"
    - "@omni-oss/runtime-utils"
    - "@omni-oss/log"
    - "@omni-oss/console-log-interceptor"

tasks:
  test:unit:
    enabled: false

  test:integration:
    enabled: true

  test:
    enabled: true

  build:
    enabled: true

  publish:
    enabled: false

meta:
  publish: false

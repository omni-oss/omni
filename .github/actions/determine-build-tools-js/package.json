{
    "name": "determine-build-steps",
    "version": "1.0.0",
    "description": "Determines build steps based on build aggregation data.",
    "type": "module",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@actions/core": "^2.0.3"
    }
}
